import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  itemContainer: {
    flexDirection: 'row'
  },

  item: {
    padding: 5
  },

  button: {
    padding: 5
  }
});

export default styles;