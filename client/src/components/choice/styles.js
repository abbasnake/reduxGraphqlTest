import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 0.1,
    flexDirection: 'row',
    alignItems: 'center',
    margin: 5
  },

  button: {
    borderWidth: 1,
    borderRadius: 1,
    margin: 5
  }
});

export default styles;